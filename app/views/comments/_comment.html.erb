


<div class="row">
  <div class="col-md-8">
     <p><%= markdown comment.body %></p>
   <small>
  <%= image_tag(comment.user.avatar.tiny.url) if comment.user.avatar? %> submitted <%= time_ago_in_words(comment.created_at) %> ago by
     <%= comment.user.name %>
     </small>

  </div>

</div>